# 大作业包装说明

## 项目定位

本项目不是单纯复现一个摘要算法，而是把 360°视频摘要论文中的三阶段系统做成一个可运行的课堂演示系统：

1. 从 360°视频中生成 saliency maps。
2. 根据显著区域自动裁剪普通 2D event video。
3. 再对 event video 做时间摘要，导出最终短 2D summary video。

这样答辩时可以明确说明：CA-SUM-360 / S³-360 是算法基础，我们的工作是把论文流程工程化、可视化、可交互化，并补齐最终可播放结果。

## 三阶段流程

### Step 1: Saliency Maps

输入：360° ERP 视频帧。

输出：每一帧的显著性热力图，表示用户最可能关注的区域。

项目中体现为：

- 原始全景帧展示。
- saliency heatmap 展示。
- 推荐视角框展示。

### Step 2: 2D Event Video

输入：原始 360°视频帧和显著性热力图。

处理：

- 找出高显著性区域。
- 按片段聚合 event。
- 围绕推荐视角裁剪普通 16:9 画面。

输出：`step2_2d_event_video.mp4`。

这个输出已经不是 360°视频，而是普通屏幕可以直接观看的 2D event video。它保留所有重要事件，但还没有做最终时间摘要。

### Step 3: Final Output

输入：Step 2 得到的 event 片段。

处理：使用 CA-SUM / S³-360 风格的时间摘要方法，从 event video 中选出最关键、少重复、覆盖事件更完整的片段。

输出：

- `step3_final_summary.mp4`：最终短 2D 视频。
- `s3_360_summary.gif`：带热力图和视角框的可视化动图。
- `metrics.csv`：方法对比指标。

## 相比复现的提升

- 增加了完整三阶段可视化页面，而不是只输出指标。
- 增加了 2D event video 这个中间结果，便于解释论文流程。
- 增加了最终短 2D MP4 导出，答辩时可以直接播放。
- 增加了交互式 360°/VR 浏览模式，可验证推荐视角是否合理。
- 增加了多方法对比、事件覆盖率、重复率和视角跳变等指标。
- 支持上传真实 360°视频，也支持 NPZ/HDF5 实验数据。

## 四人工作量拆分

### 成员 A：论文复现与系统建模

- 阅读论文和 CA-SUM-360 仓库。
- 梳理 Saliency Maps、2D Event Video、Final Summary 三阶段流程。
- 完成视频片段划分、特征读取、显著性和重要性打分。

### 成员 B：2D Event Video 生成

- 根据显著区域确定推荐视角。
- 将 360° ERP 帧裁剪成普通 16:9 画面。
- 实现 Step 2 的 event video MP4 导出。

### 成员 C：时间摘要与实验评估

- 实现 Uniform、Saliency-only、Importance-only、Saliency+Importance、S³-360 等对比方法。
- 完成 Precision、Recall、F-score、重复率、事件覆盖率、视角跳变等指标。
- 实现最终 summary video 和 storyboard GIF 导出。

### 成员 D：系统包装与交互展示

- 搭建 Streamlit 可视化页面。
- 实现 360°/VR 交互预览模式。
- 整理 README、演示流程和答辩说明。
- 准备测试视频和最终展示材料。

## 推荐答辩演示顺序

1. 先说明痛点：360°视频内容多，用户不知道看哪里，也不想完整看完。
2. 展示 Step 1：系统先判断每一帧哪里重要。
3. 展示 Step 2：系统把 360°视频裁剪成普通 2D event video。
4. 展示 Step 3：系统再做时间摘要，导出最终短 2D 视频。
5. 展示 VR 交互预览：说明这是在复现基础上的可视化增强。
6. 展示方法对比表：说明不是只做页面，而是有实验指标支撑。

## 一句话总结

我们复现了 360°视频时空摘要的核心流程，并进一步做成了一个可以上传视频、查看显著性、生成 2D event video、导出最终短视频、交互式验证推荐视角的完整系统。
